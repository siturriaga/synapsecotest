[build]
  # No build step – you're serving plain index.html + JS
  command = ""
  publish = "."

[functions]
  # Folder where your Netlify functions live
  directory = "netlify/functions"
  node_bundler = "esbuild"
